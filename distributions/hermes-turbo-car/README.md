# Hermes Turbo Agent Car

Install locally:

```bash
hermes profile install ./distributions/hermes-turbo-car --name hermes-turbo-car --alias
```

This profile is tuned for:

- quick capture while mobile
- voice-first summaries and reminders
- safe handoff of work to deeper execution profiles
