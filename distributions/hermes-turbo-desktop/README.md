# Hermes Turbo Agent Desktop

Install locally:

```bash
hermes profile install ./distributions/hermes-turbo-desktop --name hermes-turbo-desktop --alias
```

This profile is tuned for:

- coding and repository operations
- browser and desktop automation
- validation, review, and release execution
