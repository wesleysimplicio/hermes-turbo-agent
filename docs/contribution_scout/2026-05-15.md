# Hermes Daily Contribution Scout — 2026-05-15

Run context:

- Date: 2026-05-15 (America/Sao_Paulo)
- Workspace: `C:\Users\wesley.simplicio\Pictures\m\hermes-agent`
- Branch: `codex/hermes-agent-10x-fast`
- HEAD: `4d5aba9d0` (`fix(runtime): restore mock flush path and subagent heartbeat cadence`)
- `origin/main`: `a1c316c6` (`chore: add CODEOWNERS requiring @wesleysimplicio approval`)
- `upstream/main`: `ddb8d8fa` (`docs: update NovitaAI provider positioning (#25532)`)
- Divergence (local remote-tracking refs may be stale):
  - `upstream/main...HEAD`: `511 17` (left/right counts from `git rev-list --left-right --count upstream/main...HEAD`)
  - `origin/main...HEAD`: `0 16` (left/right counts from `git rev-list --left-right --count origin/main...HEAD`)

## Upstream/fork sync (blocked)

This run could not fetch “latest” refs via git due to network restrictions in the current shell environment (cannot reach `github.com:443`).

```powershell
git fetch --no-write-fetch-head --prune upstream
# fatal: unable to access 'https://github.com/NousResearch/hermes-agent.git/': Failed to connect to github.com port 443 ... Could not connect to server
```

```powershell
git fetch --no-write-fetch-head --prune origin
# fatal: unable to access 'https://github.com/wesleysimplicio/hermes-agent.git/': Failed to connect to github.com port 443 ... Could not connect to server
```

Implication: local `origin/*` and `upstream/*` remote-tracking refs may be stale. The “PR snapshot” below is taken from GitHub web snapshots where possible.

## Open PR log snapshot (NousResearch/hermes-agent)

Snapshot source: GitHub PR list (open PRs): https://github.com/NousResearch/hermes-agent/pulls

Note: this environment can browse the web, but cannot `git fetch`, so treat the list below as a “web snapshot” (it may not exactly match the repo state on 2026-05-15).

Sample from the top of the open-PR list (as crawled by the web tool):

- #21762 `fix(cron): instruct agent that response is delivered verbatim, no preamble` (opened May 8, 2026)
- #21761 `fix(auxiliary): recover Codex stream drops` (opened May 8, 2026)
- #21759 `feat(slack): explicit acceptance before ✅ thread status` (opened May 8, 2026)
- #21758 `fix(gateway): isolate WhatsApp identities and targets` (opened May 8, 2026)
- #21756 `docs: add Kanban Codex lane skill` (opened May 8, 2026)
- #21755 `fix(docker): chown virtualenv for non-root runtime` (opened May 8, 2026)
- #21754 `fix(feishu): support progress message cleanup` (opened May 8, 2026)
- #21753 `fix(tui): avoid unsupported Cmd hotkeys in macOS help` (opened May 8, 2026)
- #21752 `fix(hindsight): sanitize base64 data URLs before retain` (opened May 8, 2026)
- #21750 `fix(gateway): mirror background deliveries into sessions` (opened May 8, 2026)
- #21749 `feat(dashboard): add Spanish, French, German, Korean and Russian i18n support` (opened May 8, 2026)
- #21748 `fix(gateway): handle sessions slash command` (opened May 8, 2026)
- #21747 `feat: support custom base_url for Gemini native adapter` (opened May 8, 2026)
- #21745 `fix: add cross-container gateway health detection` (opened May 8, 2026)
- #21744 `fix(webhook): filter route actions before agent dispatch` (opened May 8, 2026)
- #21743 `fix(auth): read api_key from config.yaml for built-in providers — fixes #21725` (opened May 8, 2026)
- #21742 `fix(gateway): handle sessions command` (opened May 8, 2026)
- #21740 `fix(config): preserve custom provider max_output_tokens` (opened May 8, 2026)
- #21739 `fix(skills): replace symlink install destinations` (opened May 8, 2026)

## Open PR log snapshot (wesleysimplicio/hermes-agent)

Status: no reliable snapshot available in this environment.

- GitHub web search did not surface a public PR list page for `wesleysimplicio/hermes-agent` (the fork may be private and/or not indexed).
- Shell git cannot reach GitHub to query PR metadata via `gh`/API or to fetch refs from `origin`.

## Recent merged/closed pattern notes (NousResearch/hermes-agent)

Using official upstream release notes as a proxy for “recent merged PR trends”:

- Release highlights show a heavy concentration in:
  - Self-maintenance / “Curator” and self-improvement loop upgrades
  - Gateway/platform surface area (more platforms, better session continuity, safer delivery)
  - Provider/auth support (more providers, better auth flows)
  - TUI and runtime performance improvements

Source (official): https://github.com/NousResearch/hermes-agent/releases

## Duplicate-risk map (avoid redundant PRs)

Based on the open-PR sample above, the following areas look collision-prone today:

- “Sessions command” gateway fixes: multiple open PRs with near-identical titles (`handle sessions slash command` vs `handle sessions command`).
- Gateway delivery/session mirroring: changes around “background deliveries into sessions” are likely to overlap with session-command work.
- Config/auth correctness: provider `api_key` + token budget/max tokens interactions (high chance of concurrent fixes).

Safe contribution posture: prefer reviews/comments that consolidate PRs in the clusters above (or small surgical follow-ups once one PR lands).

## Prioritized review candidates (high value, low duplication)

These are biased toward bug fixes with clear repro surfaces and likely mergeability:

1) `fix(auxiliary): recover Codex stream drops` (#21761)
- Why: streaming robustness is user-visible; touches auxiliary path (often brittle).
- Review focus: minimal behavioral change, add/confirm regression test, ensure fallback chain semantics stay correct.

2) `fix(auth): read api_key from config.yaml for built-in providers` (#21743)
- Why: auth correctness is high-severity UX; may unblock many “it works locally but not in gateway” reports.
- Review focus: avoid persisting secrets unexpectedly; confirm precedence rules (env vs config vs auth.json) remain stable.

3) `fix(docker): chown virtualenv for non-root runtime` (#21755)
- Why: container installs are a first-contact path; non-root correctness reduces support burden.
- Review focus: idempotency, security (don’t loosen perms), CI coverage.

4) `fix(gateway): isolate WhatsApp identities and targets` (#21758)
- Why: cross-identity leakage is operationally dangerous; likely to be prioritized.
- Review focus: multi-account routing + session attribution invariants; add regression if possible.

5) `fix(config): preserve custom provider max_output_tokens` (#21740)
- Why: config drift/regressions are frequent and hard to debug.
- Review focus: migration and deep-merge behavior; ensure no breaking changes to existing configs.

## Hermes vs OpenClaw / Claude Code / Codex (official-source-only notes)

### OpenClaw (official docs)

- OpenClaw exposes a broad CLI surface, including reference docs for core commands and global flags. (docs.openclaw.ai)
- OpenClaw documents a managed browser profile and “browser” integration explicitly. (docs.openclaw.ai)

References:
- https://docs.openclaw.ai/start/getting-started
- https://docs.openclaw.ai/cli
- https://docs.openclaw.ai/browser

### Claude Code (official docs)

- Claude Code positions itself as a terminal-first agentic coding system; docs emphasize settings and integration surfaces like MCP. (Anthropic)

References:
- https://docs.anthropic.com/en/docs/claude-code/overview
- https://docs.anthropic.com/en/docs/claude-code/settings
- https://code.claude.com/docs/en/commands

### Codex CLI / Codex (official docs)

- Codex CLI is an open-source terminal coding agent that can read/edit/run code locally. (OpenAI)
- Codex product guidance emphasizes evidence/verification constraints (tests/logs, limited internet access in some modes). (OpenAI)

References:
- https://help.openai.com/en/articles/11096431-openai-codex-ci-getting-started
- https://openai.com/index/introducing-codex/
- https://github.com/openai/codex

## Small, safe Hermes improvement ideas (prefer consolidation)

Given the current PR clusters, the safest incremental improvements are *docs/UX* and “consolidation helpers”:

- Docs: add a “how to review overlapping PRs” section to the contributing guide (suggest labeling + how to mark duplicates kindly).
- Docs: explicitly document credential precedence for built-in providers (config vs env vs auth.json) to reduce duplicate bug PRs in auth/config.
- CLI UX: add a tiny “evidence footer” template to encourage every agent run to end with (tests run, key commands, files changed) — as docs-only guidance first, before any CLI code change.

## Test evidence

No tests were run in this environment:

- `python` is not available (`python`/`py` not found), so `scripts/run_tests.sh` cannot execute here.
- This run produced docs-only output (new scouting note).

## Blockers

- Outbound GitHub network blocked from shell (`git fetch` fails to connect to `github.com:443`).
- Fork PR snapshot not available via web tooling (repo may be private / not indexed).

## Next actions (when network is available)

1) Refresh local refs:

```powershell
git fetch --prune upstream
git fetch --prune origin
git log -1 --oneline upstream/main
git log -1 --oneline origin/main
```

2) Replace the PR snapshot section with a truly current list:
- Prefer `gh pr list --repo NousResearch/hermes-agent --state open --limit 50` (or GitHub API) once network is unblocked.

